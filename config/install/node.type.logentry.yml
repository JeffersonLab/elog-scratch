langcode: en
status: true
dependencies:
  # The enforced block will remove the content-type when the module
  # is deleted.  Without this, the content type will remain after module
  # has been uninstalled.
  enforced:
    module:
      - elog
  module:
    - menu_ui
third_party_settings:
  menu_ui:
    available_menus:
      - main
    parent: 'main:'
name: Log Entry
type: logentry
description: 'Electronic Logbook Entry'
help: ''
new_revision: true
preview_mode: 1
display_submitted: true